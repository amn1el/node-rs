[package]
  name = "humanize"
  version = "0.1.0"
  edition = "2024"

[lib]
  crate-type = ["cdylib"]

[dependencies]
  napi_allocator = { workspace = true }

  napi = { workspace = true }
  napi-derive = { workspace = true }

[build-dependencies]
  napi-build = { workspace = true }
